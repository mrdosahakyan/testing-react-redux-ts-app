import styled from "styled-components";
import { Col } from "antd";

export const SInputWrapper = styled(Col)`
  background-color: white;
  border-radius: 0.25rem;
  padding: 1rem;
  box-shadow: 0.1rem 0.1rem 1rem rgba(0, 0, 0, 0.329);
`;
